# Production Environment Variables
# These should be set in your deployment platform (Vercel, etc.)

# Upstash Redis (Production)
# Get these from Upstash Console → Your Database → REST API tab
KV_URL=your-production-upstash-rest-url
UPSTASH_REDIS_REST_TOKEN=your-production-upstash-rest-token
